if(NOT H2D_REAL)
    return()
endif(NOT H2D_REAL)
project(test-zoom-to-fit)

add_executable(${PROJECT_NAME} main.cpp)
include (${hermes2d_SOURCE_DIR}/CMake.common)
set_common_target_properties(${PROJECT_NAME})

set(BIN ${PROJECT_BINARY_DIR}/${PROJECT_NAME})
add_test(test-zoom-to-fit_const-on-square "${BIN}" 0 square.mesh)
add_test(test-zoom-to-fit_const-on-distorted "${BIN}" 0 distorted.mesh)
add_test(test-zoom-to-fit_plane-on-square "${BIN}" 1 square.mesh)
add_test(test-zoom-to-fit_plane-on-distorted "${BIN}" 1 distorted.mesh)
add_test(test-zoom-to-fit_cuboid-on-square "${BIN}" 2 square.mesh)
add_test(test-zoom-to-fit_cuboid-on-curved "${BIN}" 2 curved.mesh)
add_test(test-zoom-to-fit_bigrange-on-square "${BIN}" 3 square.mesh)
add_test(test-zoom-to-fit_bigrange-on-curved "${BIN}" 3 curved.mesh)
add_test(test-zoom-to-fit_paraboloid-on-square "${BIN}" 4 square.mesh)

set_tests_properties(test-zoom-to-fit_const-on-square PROPERTIES LABELS slow)
set_tests_properties(test-zoom-to-fit_const-on-distorted PROPERTIES LABELS slow)
set_tests_properties(test-zoom-to-fit_plane-on-square PROPERTIES LABELS slow)
set_tests_properties(test-zoom-to-fit_plane-on-distorted PROPERTIES LABELS slow)
set_tests_properties(test-zoom-to-fit_cuboid-on-square PROPERTIES LABELS slow)
set_tests_properties(test-zoom-to-fit_cuboid-on-curved PROPERTIES LABELS slow)
set_tests_properties(test-zoom-to-fit_bigrange-on-square PROPERTIES LABELS slow)
set_tests_properties(test-zoom-to-fit_bigrange-on-curved PROPERTIES LABELS slow)
set_tests_properties(test-zoom-to-fit_paraboloid-on-square PROPERTIES LABELS slow)

